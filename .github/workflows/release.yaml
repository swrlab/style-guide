name: Release NPM package

on:
  push:
    branches: [feat/add-changesets-for-publishing]

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    if: github.repository == 'swrlab/style-guide'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      id-token: write
      attestations: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Setup bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.9
          registries: |
            https://registry.npmjs.org/

      - name: Setup node for npm OIDC
        uses: actions/setup-node@395ad3262231945c25e8478fd5baf05154b1d79f # v6.0.1
        with:
          node-version: 24.x

      - name: bun install
        run: bun install --frozen-lockfile

      - name: bun build
        run: bun run build

      - name: bun test
        run: bun run --bun test

      - name: Create Release Pull Request or Publish to npm with trusted publishing
        id: changesets
        uses: changesets/action@v1
        with:
          version: npm changeset:version
          publish: npm changeset:publish
          title: 'chore: release packages'
          commit: 'chore: release packages'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_CONFIG_PROVENANCE: true

      - name: create github release
        run: gh release create ${{ github.ref_name }} --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
